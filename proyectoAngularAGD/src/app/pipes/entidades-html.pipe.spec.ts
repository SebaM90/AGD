import { EntidadesHtmlPipe } from './entidades-html.pipe';

describe('EntidadesHtmlPipe', () => {
  it('create an instance', () => {
    const pipe = new EntidadesHtmlPipe();
    expect(pipe).toBeTruthy();
  });
});
